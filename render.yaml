services:
  - type: web
    name: tsn-stock
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: TSN_API_BASE_URL
        sync: false
